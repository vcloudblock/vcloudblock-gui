<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="google" value="notranslate">
    <link rel="shortcut icon" href="static/favicon.ico">
    <title><%= htmlWebpackPlugin.options.title %></title>
    <% if (htmlWebpackPlugin.options.sentryConfig) { %>
        <!-- Sentry error logging to help with finding bugs -->
        <script src="https://cdn.ravenjs.com/3.22.1/raven.min.js" crossorigin="anonymous"></script>
        <script>
            Raven.config(<%= htmlWebpackPlugin.options.sentryConfig %>).install();
        </script>
        <!-- /Sentry -->
    <% } %>
    <style>
        #pre-loading {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background-color: #4C97FF;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: -9999;
        }
        #pre-loading-text {
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            font-size: 2rem;
            font-weight: bold;
            margin: 0.75rem 0;
            color: #fff;
        }
        #pre-loading-spinner {
            border: 0.4rem solid #E5F0FF;
            border-radius: 50%;
            border-top: 0.4rem solid #79B1FF;
            width: 3rem;
            height: 3rem;
            animation: spin 2s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
  </head>
  <body>
    <div id="pre-loading">
        <div id="pre-loading-text">Loading...</div>
      <div id="pre-loading-spinner"></div>
    </div>
  </body>
</html>
